# Handoff — LAB0-03 Ubuntu success

## Assignment

`STACK-01` remains human-blocked. This increment therefore closes the next dependency-safe evidence row: one measured Ubuntu 24.04 local-native Lab 0 setup/build/launch path.

## Verified evidence

- Lab 0 workflow run `29622240261`: success.
- Documentation CI run `29622240365`: success.
- Retained artifact `8422651113`, digest `sha256:2db76be20de63afb5ab080fbdca8354de60859d34d4f21970c5331f9f2871295`.
- `uv sync --locked`, pinned checkout, CMake/Ninja configure, bounded `llama-cli` build and `llama-cli --help` all passed.
- Time to ready: 326,905 ms.
- No model or inference; no diagnostics, secrets, personal paths, telemetry or learner data.

## Claim supported

One clean Ubuntu 24.04/x86_64 environment can complete the model-free local-native Lab 0 setup/build/launch contract.

## Claim not supported

This does not establish model loading, inference, time to first token, offline operation, devcontainer/macOS/WSL2 equivalence, educational effectiveness or independent technical correctness.

## Status changes

- `LAB0-03`: `evidenced` for the Ubuntu row.
- `LAB0-04`: next dependency-safe validation task when `STACK-01` remains blocked.
- `DEMO-01`: still blocked by canonical integration, devcontainer evidence and canonical progress integration.

## Human action

Approve PR #24 as the canonical progress base or record a different integration decision. Nominate an independent llama.cpp/GGML reviewer before technical-correctness closure.

## Next agent action

Return immediately to `STACK-01` after human approval. Otherwise execute the same retained model-free path in the devcontainer/Codespaces-compatible environment without adding inference, models or broad features.