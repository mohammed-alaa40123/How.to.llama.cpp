[project]
name = "how-to-llama-cpp-learning-tools"
version = "0.0.0"
description = "Dependency-free validation tooling for the executable-learning repository"
requires-python = ">=3.11"

[tool.uv]
package = false
